import Test from "../firebase/test";

const Setting = () => {
  return (
    <section className="flex bg-amber-900 min-h-[calc(100vh-8rem)] sm:min-h-[calc(100vh-4rem)] items-center justify-center">
      <Test/>
    </section>
  );
};

export default Setting;
